%h1 Listing sessions

%table
  %tr
    %th Date
    %th Wind speed
    %th Comments
    %th Kite
    %th Board
    %th Location
    %th
    %th
    %th

  - @sessions.each do |session|
    %tr
      %td= session.date
      %td= session.wind_speed
      %td= session.comments
      %td= session.kite.name rescue '--'
      %td= session.board.name rescue '--'
      %td= session.location.name rescue '--'
      %td= link_to 'Show', session
      %td= link_to 'Edit', edit_session_path(session)
      %td= link_to 'Destroy', session, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Session', new_session_path
